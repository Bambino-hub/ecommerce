{% extends 'base.html.twig' %} {% block title %}Product{% endblock %} {% block
body %}
<h1>Product</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ product.id }}</td>
    </tr>
    <tr>
      <th>Name</th>
      <td>{{ product.name }}</td>
    </tr>
    <tr>
      <th>Discription</th>
      <td>{{ product.discription }}</td>
    </tr>
    <tr>
      <th>Price</th>
      <td>{{ product.price }}</td>
    </tr>
    <tr>
      <th>Image</th>
      <td>
        <img src="{{ vich_uploader_asset(product, "imagefile") }}"
        alt="l'image">
      </td>
    </tr>
  </tbody>
</table>

<a class="btn btn-primary" href="{{ path('app_product_index') }}"
  >back to list</a
>

<a
  class="btn btn-warning"
  href="{{ path('app_product_edit', { id: product.id }) }}"
  >edit</a
>
<a class="btn btn-secondary" href="{{ path('app_histry', { id: product.id }) }}"
  >Voir l'historique</a
>

{{ include("product/_delete_form.html.twig") }}
{% endblock %}

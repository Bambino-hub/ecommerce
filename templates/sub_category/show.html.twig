{% extends 'base.html.twig' %} {% block title %}SubCategory{% endblock %} {%
block body %}
<h1>La sous Catégorie</h1>

<table class="table">
  <thead>
    <tr>
      <th>id</th>
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>{{ sub_category.id }}</td>
      <td>{{ sub_category.name }}</td>
      <td>
        <div class="row">
          <div class="col">
            <a
              class="btn btn-primary"
              href="{{
                path('app_sub_category_edit', { id: sub_category.id })
              }}"
              >edit</a
            >
          </div>
          <div class="col">
            {{ include("sub_category/_delete_form.html.twig") }}
          </div>
        </div>
      </td>
    </tr>
  </tbody>
</table>
<a class="btn btn-primary" href="{{ path('app_sub_category_index') }}"
  >back to list</a
>
{% endblock %}
